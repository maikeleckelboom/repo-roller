# Enable npm provenance for supply chain security
# This adds cryptographic proof of where the package was built
provenance=true

# Use legacy-peer-deps to handle React 18/19 compatibility
legacy-peer-deps=true
